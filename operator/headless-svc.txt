resource "kubernetes_manifest" "kafka_bootstrap_headless" {
  manifest = {
    apiVersion = "v1"
    kind       = "Service"
    metadata = {
      name      = "my-kafka-cluster-kafka-bootstrap-headless"
      namespace = "strimzi"
    }
    spec = {
      clusterIP = "None"
      ports = [
        {
          port       = 9092
          targetPort = 9092
          protocol   = "TCP"
        }
      ]
      selector = {
        "strimzi.io/cluster" = "my-kafka-cluster"
        "strimzi.io/kind"    = "Kafka"
      }
    }
  }
}
